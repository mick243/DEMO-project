# CLAUDE.md

강정민 포트폴리오 사이트 (Next.js 14 App Router). 원격은 **공개** 저장소 `mick243/DEMO-project`, 기본 브랜치는 **`master`** 입니다.

- **지금 상태의 정본은 루트 `SSOT.md`** 입니다. 브랜치 · 워크트리 · 콘텐츠 수치 · 미해결 · 다음 작업은 거기서 봅니다.
- 페이지 구조와 컴포넌트 역할은 `README.md` 가 정본입니다.
- 화면에 뜨는 글과 수치는 전부 `app/data/*.js` 에 있습니다. 컴포넌트에 문구를 박아 넣지 않습니다.
- 상시 규칙은 `.claude/rules/` 에 있고 매 세션 자동으로 읽힙니다.
  - `daily-ssot-update.md` — 매일 18:50 에 진행 상태를 다시 재고 `SSOT.md` 를 갱신합니다.

## 이 저장소에서 조심할 것

- **공개 저장소입니다.** 커밋에 개인 정보나 키가 섞이지 않는지 봅니다. push 는 곧 게시입니다.
- **`backup/*` 브랜치는 지우지 않습니다.** node_modules 를 이력에서 걷어내기 전의 유일한 사본입니다.
- 사이트에 실린 오락실 파인더의 기능·성능 수치는 **다른 저장소의 값을 옮겨 실은 콘텐츠**입니다. 여기서 다시 재려 하지 말고 출처를 확인합니다.
- 09-07 이후로는 `master` 에 직접 커밋하지 않고 브랜치 → PR 로만 들였습니다.
